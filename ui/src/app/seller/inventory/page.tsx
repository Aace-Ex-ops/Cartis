import { PagePlaceholder } from "@/components/shared/page-placeholder";

export default function Page() {
  return <PagePlaceholder title="Inventory" note="Inventory table, COGS, reorder alerts." />;
}
